when@dev:
    web_profiler:
        toolbar: true

    framework:
        profiler:
            collect_serializer_data: true

when@test:
    framework:
        profiler:
            dsn: 'file:%kernel.project_dir%/var/cache/dev/profiler'
            collect: false
            collect_serializer_data: true