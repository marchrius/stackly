name: Release container image

on:
  push:
    branches:
      - develop
      - main
      - master
    tags:
      - '**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build and push image
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || github.ref_type == 'tag' || contains(github.event.head_commit.message, '[do-release]')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read package version
        id: package
        shell: bash
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid package.json version for SemVer: $VERSION" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "image=${REGISTRY}/${IMAGE_NAME,,}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata for scratch image
        id: meta-scratch
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.package.outputs.image }}
          tags: |
            type=raw,value=${{ steps.package.outputs.version }}
            type=raw,value=v${{ steps.package.outputs.version }}
            type=sha,format=short
          labels: |
            org.opencontainers.image.title=Stackly
            org.opencontainers.image.description=Self-hosted collection manager for physical collections, metadata, wishlists, albums, and loans.
            org.opencontainers.image.version=${{ steps.package.outputs.version }}
            org.opencontainers.image.ref.name=scratch

      - name: Extract Docker metadata for Alpine image
        id: meta-alpine
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.package.outputs.image }}
          tags: |
            type=raw,value=${{ steps.package.outputs.version }}-alpine
            type=raw,value=v${{ steps.package.outputs.version }}-alpine
            type=sha,format=short,suffix=-alpine
          labels: |
            org.opencontainers.image.title=Stackly Alpine
            org.opencontainers.image.description=Self-hosted collection manager for physical collections, metadata, wishlists, albums, and loans.
            org.opencontainers.image.version=${{ steps.package.outputs.version }}
            org.opencontainers.image.ref.name=alpine

      - name: Build and push scratch image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.scratch
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-scratch.outputs.tags }}
          labels: ${{ steps.meta-scratch.outputs.labels }}
          cache-from: type=gha,scope=scratch
          cache-to: type=gha,mode=max,scope=scratch

      - name: Build and push Alpine image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta-alpine.outputs.tags }}
          labels: ${{ steps.meta-alpine.outputs.labels }}
          cache-from: type=gha,scope=alpine
          cache-to: type=gha,mode=max,scope=alpine
